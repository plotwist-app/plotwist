export type Dictionary = {
  home: {
    title: string
    description: string
    start: string
    read_more: string
  }
  movies_list: {
    now_playing: string
    popular: string
    top_rated: string
    upcoming: string
    show_all: string
  }
}
